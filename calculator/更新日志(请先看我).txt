此更新日志为了方便同学再次批改
10.17
9.29:
1. 现在不允许输入以0开头的数字（八进制）了，将其视为错误
2. 现在不允许重复输入+-*/等操作符了，输入多次视为一次（去除了**作为pow的功能）
3. 设置了结果的小数点精确到16位，以修复0.1+0.2或0.2*0.2等bug
Tips: 计算完成后保留结果而不被清空是被老师认可的机制，因此不做改动

10.12：
1. 修复了直接输入=输出underfined的漏洞
2. 现在不再允许操作符连续输入，若需要，则加括号。

21.48：
1. 加粗了按钮中的字，使其变得更显眼。

23.18：
1. 根据同学建议取消保留结果机制，显示结果时在前加入=符号以方便辨识，同时将小数点精确位缩小至15位以对齐显示框